def draw_triangle(rows):
    no_of_rows = rows
    k = no_of_rows - 1
    for i in range(1, no_of_rows + 1):
        for j in range(k):    # 4, 3, 2, 1, 0
            print(" ",end="")
        for l in range(i):    # 1, 2, 3, 4, 5
            print("* ",end="")
        k -= 1
        print()
